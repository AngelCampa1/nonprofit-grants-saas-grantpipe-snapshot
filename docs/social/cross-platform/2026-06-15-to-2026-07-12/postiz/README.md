# Postiz Package

Corrected GrantPipe package for June 15 to July 12, 2026.

- Rows: 252 platform posts.
- Groups: 84 Postiz CLI payloads.
- Cadence: 3 LinkedIn, 3 X, and 3 Threads posts per local day.
- Scheduler: `scripts/postiz-cli-group-scheduler.mjs` from `<sibling repo>`.

The prior 84-row package was removed and replaced because it scheduled 3 posts per day across all platforms instead of per platform.
