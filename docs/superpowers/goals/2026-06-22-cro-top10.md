# Goal: CRO on top-10 most-visited public pages (+ signup)

Started 2026-06-22. Worktree `cro-top10-pages`. Sub-agent driven. Iterate review/fix until clean.

## Data source

Google Search Console (sc-domain:grantpipe.com, 90d). PostHog MCP not loaded this session.

- Homepage `/` — #1 organic clicks (34 clicks / 547 impr).
- Highest reach = template pages: `compare/pricing/[slug]` (Bloomerang 3070, Blackbaud 1519, Salesforce 1958, Neon 2360), `resources/guides/[slug]` (Salesforce-cost 3454, CHAR500 2204, procurement 2380, single-audit 1242).
- Optimizing a template lifts every instance → highest leverage.

## Target list (locked)

1. Homepage — `apps/site/src/pages/index.astro`
2. Signup — `apps/web/src/routes/signup.tsx` (explicitly required by goal)
3. Pricing — `apps/site/src/pages/pricing.astro`
4. Product — `apps/site/src/pages/product.astro`
5. Compare/pricing template — `apps/site/src/pages/compare/pricing/[slug].astro` (+ data)
6. Resources/guides template — `apps/site/src/pages/resources/guides/[slug].astro`
7. Compare/versus template — `apps/site/src/pages/compare/versus/[slugA]-vs-[slugB].astro`
8. Free tools / calculators — `apps/site/src/pages/free/*` (lead-gen)
9. Features template — `apps/site/src/pages/features/[slug].astro`
10. Money landing — `apps/site/src/pages/grant-management-software.astro`

## Guardrails (must hold)

- Buttons are pills (rounded-full).
- Verified facts: de minimis 15% MTDC, single audit $1M, MTDC subaward cap $50K, equipment $10K. FFATA/SAM remain $25K.
- No lies: no fabricated numbers/testimonials/logos/user counts. Builder perspective, no nonprofit-sector-experience claims.
- Copy passes: humanizer → third-grade-copy on every changed user-facing string.
- Observability: any new interactive/conversion element wires PostHog + Sentry per CLAUDE.md.

## Round 2 — corrected with REAL PostHog traffic (project 390138, 30d)

The original GSC-based target list was a proxy. Pulled real pageviews via PostHog
HogQL (key with query scope; 30d window, no host filter — 90d+host filter times out
at the 10s exec cap). Public pages only, excluding `/app/*` operational:

- `/app/signup` 62, `/` home 52, `compare/pricing/salesforce-nonprofit` 18,
  `compare/alternatives/salesforce-nonprofit` 16, `/app/login` 14, guides 13,
  `/product/` 13, `/pricing/` 13, `/about/` 7.
- `/lp/*` had ZERO real traffic — the earlier guess was wrong; dropped from scope.

Real gaps found + fixed this round:

- **compare/alternatives/[slug].astro** — was missing the editorial-panel pill CTA
  its `compare/pricing` sibling has. Added it (data-cta-page-family="compare-alternatives").
- **about.astro** — `knowsAbout` schema claimed personal nonprofit-accounting / FASB
  expertise the founder lacks → reworded to builder/product-accurate terms. Body
  "world-class, personalized service" superlative → "personal, hands-on service".
- **login.tsx** — brought to signup parity: password show/hide, inline email/password
  blur validation, Sentry `captureAppException` on catch (tags source=login,
  feature=email-login). +7 tests, login.tsx 100% per-file coverage.
- **index.astro (home)** — split the 40-word hero lede run-on into 3 clean sentences.
- **pricing.astro / pricing-plan-cards.astro** — left as-is. Already clean; the
  "Focused path" badge is the intentional muted label, not a contradiction. No price changes.

## Status

- [x] Phase 0 audit (real-traffic corrected)
- [x] Phase 1 implement + copy passes (humanizer → third-grade-copy → no-lies → context-fit)
- [x] Phase 2 review/fix — code review APPROVE, no issues
- [x] Phase 3 build/verify/merge/deploy — merged to master (1f618e60),
      worktree removed, deployed: site `1edfe593` (grantpipe.com) + web `9f74a6ee`
      (app.grantpipe.com). Live smoke check 200 on /, /about/, /login; new /about/
      copy confirmed live. Pre-existing working-tree changes committed separately
      (14b9ba42) and pushed.

Goal complete 2026-06-22.
