# AGENTS.md — GrantPipe

Repository guidance for agentic coding tools lives in **[`CLAUDE.md`](./CLAUDE.md)**.

This file previously duplicated that content and drifted out of sync with it. It is now a
pointer so there is exactly one source of truth. Tools that look for `AGENTS.md` should read
`CLAUDE.md` instead.
